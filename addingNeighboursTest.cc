#include "addingneighbours.h"

int main()
{
	Node *root1 = new Node;
	Node *leftNode = new Node;
	Node *rightNode = new Node;
	root1->left = leftNode;
	root1->right = rightNode;
	
	return 0;
}